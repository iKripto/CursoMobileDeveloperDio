console.log("Olá, mundo");
console.log("Bruce Wayne.");
// node --watch arquivo.js;
// Serve para ficar atualizando e rodando o programa toda vez que apertar para salvar.

// Comandos para navegação de diretórios:
// del *                        (delete) deleta arquivos.
// ls                           (list) lista arquivos no diretório.
// cls                          (clear) limpa terminal.
// cd                           (change directory) muda diretório.
// mkdir                        (make directory) cria um novo diretório.

// Comandos npm e node.
// npm init -y                  inicia um projeto node.
// node [arquivo]               roda um arquivo javascript.
// node --watch [arquivo]       node fica vigiando o arquivo.